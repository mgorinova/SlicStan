def beta_mean_count(real mean, real total_count){
    real alpha = total_count * mean;
    real beta = total_count * ( 1 - mean );
    return ~ beta(alpha, beta);
}

real phi ~ beta(1, 1); 
real lambda ~ pareto(0.1, 1.5);

data vector[10] theta ~ beta_mean_count( phi, lambda );